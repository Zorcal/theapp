# clients

Thin wrappers around third-party APIs and services. Each sub-package exposes only the operations this application needs and hides the underlying SDK or HTTP client behind an interface, so the provider can be swapped without touching call sites.
